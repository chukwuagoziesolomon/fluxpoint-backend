# 🎨 Visual Architecture Diagrams

## Complete No-Code Strategy Builder Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                              │
│  User types: "Buy when RSI < 30 and price crosses above 50 EMA"    │
└───────────────────────────────┬─────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    NLP PARSER (LLM + Regex)                         │
│  ┌───────────────┐                                                  │
│  │ OpenRouter API│  Parse natural language → structured rules       │
│  │ Mistral/Claude│  Fallback to regex if LLM fails                 │
│  └───────────────┘                                                  │
└───────────────────────────────┬─────────────────────────────────────┘
                                │
                                ▼
                    ┌──────────────────────┐
                    │   Structured Rules    │
                    │ ─────────────────────│
                    │ indicators: [...]    │
                    │ entry_conditions:[...]│
                    │ exit_conditions: [...]│
                    │ risk_management: {}  │
                    └───────────┬──────────┘
                                │
            ┌───────────────────┼───────────────────┐
            │                   │                   │
            ▼                   ▼                   ▼
   ┌────────────────┐  ┌────────────────┐  ┌────────────────┐
   │    DATABASE    │  │  RULE ENGINE   │  │  FEATURES      │
   │   (Storage)    │  │  (Execution)   │  │  (Auto-Gen)    │
   │ ─────────────  │  │ ─────────────  │  │ ──────────────│
   │ UserStrategy   │  │ Indicators     │  │ From user's    │
   │ Components     │  │ Evaluator      │  │ indicators     │
   │ Indicators     │  │ Risk Mgmt      │  │                │
   └────────────────┘  └────────────────┘  └────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    DATA COLLECTION PIPELINE                         │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ 1. Fetch historical data (MT5)                              │  │
│  │ 2. Calculate user's indicators (RSI, EMA, MACD, etc.)       │  │
│  │ 3. Scan history for valid entry signals                     │  │
│  │ 4. Track outcomes (TP hit first = 1, SL hit first = 0)      │  │
│  │ 5. Extract features for each setup                          │  │
│  └──────────────────────────────────────────────────────────────┘  │
└───────────────────────────────┬─────────────────────────────────────┘
                                │
                    ┌───────────┴───────────┐
                    │  Training Dataset     │
                    │ ────────────────────  │
                    │ X: [features...]      │
                    │ y: [1, 0, 1, 1, 0...] │
                    │ 500-1000 setups       │
                    └───────────┬───────────┘
                                │
            ┌───────────────────┼───────────────────┐
            │                                       │
            ▼                                       ▼
┌──────────────────────────────┐    ┌──────────────────────────────┐
│    LAYER 1: DEEP LEARNING    │    │   LAYER 2: REINFORCEMENT     │
│    (Probability Prediction)  │    │      LEARNING (Execution)    │
│ ───────────────────────────  │    │ ───────────────────────────  │
│                              │    │                              │
│  Input: Setup features       │    │  Input: State (ML prob +     │
│         [rsi, ema, macd...]  │    │         market context)      │
│                              │    │                              │
│  Model: DNN [128→64→32→1]    │    │  Algorithm: PPO              │
│                              │    │                              │
│  Output: P(success) ∈ [0,1]  │    │  Actions:                    │
│                              │    │    0: Enter full             │
│  Loss: Binary Cross Entropy  │    │    1: Enter half             │
│                              │    │    2: Wait/Skip              │
│  Training: 50 epochs         │    │    3: Exit trade             │
│            Early stopping    │    │    4: Trail stop             │
│                              │    │                              │
│  Filter: Only trade if       │    │  Reward: R-multiples         │
│          P >= 0.65           │    │          (TP/SL ratio)       │
│                              │    │                              │
│  Purpose: Pattern Recognition│    │  Purpose: Execution          │
│           "Will this work?"  │    │           "When/how to do it?"│
└──────────────┬───────────────┘    └──────────────┬───────────────┘
               │                                    │
               │   ✅ P=0.73 (≥0.65)               │
               └────────────────┬───────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         BACKTESTING ENGINE                          │
│  Test ML + RL on historical data (holdout set)                     │
│  Calculate: Win rate, Profit Factor, Sharpe, Max DD                │
└───────────────────────────────┬─────────────────────────────────────┘
                                │
                                ▼
                        ┌───────────────┐
                        │  Performance  │
                        │  Acceptable?  │
                        └───────┬───────┘
                                │
                        ┌───────┴───────┐
                   NO   │               │   YES
                  ────►│               │────►
                        │               │
                        ▼               ▼
              ┌──────────────┐  ┌──────────────┐
              │  Retrain or  │  │     DEPLOY   │
              │   Reject     │  │  LIVE TRADING│
              └──────────────┘  └──────┬───────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       LIVE TRADING SYSTEM                           │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ 1. Monitor market for user's signals (rule engine)          │  │
│  │ 2. Extract features when signal appears                     │  │
│  │ 3. Filter with ML model (P >= 0.65)                         │  │
│  │ 4. RL agent decides action (enter/wait/exit)                │  │
│  │ 5. Execute trade on MT5                                     │  │
│  │ 6. Monitor position (trail stops, exits)                    │  │
│  │ 7. Log results and update performance                       │  │
│  └──────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

---

## ML + RL Integration (Detailed)

```
                    LIVE CANDLE ARRIVES
                            │
                            ▼
              ┌──────────────────────────┐
              │   RULE ENGINE CHECK      │
              │  Calculate Indicators    │
              │  Evaluate Conditions     │
              └──────────┬───────────────┘
                         │
                         ▼
                 ┌───────────────┐
                 │  Entry Signal?│
                 └───────┬───────┘
                         │
                    NO   │   YES
                   ─────►│◄─────
                         │
                         ▼
              ┌──────────────────────────┐
              │  EXTRACT FEATURES        │
              │  - Indicator values      │
              │  - Market context        │
              │  - Normalized array      │
              └──────────┬───────────────┘
                         │
                         ▼
              ┌──────────────────────────┐
              │  ML MODEL (Layer 1)      │
              │  Input: Features [45D]   │
              │  Output: P(success)      │
              └──────────┬───────────────┘
                         │
                         ▼
                 ┌───────────────┐
                 │  P >= 0.65?   │
                 └───────┬───────┘
                         │
                    NO   │   YES
              Skip ◄─────┤─────► Pass
                         │
                         ▼
              ┌──────────────────────────┐
              │  BUILD RL STATE          │
              │  - ML probability        │
              │  - Account state         │
              │  - Market volatility     │
              │  - Open positions        │
              │  - Time context          │
              └──────────┬───────────────┘
                         │
                         ▼
              ┌──────────────────────────┐
              │  RL AGENT (Layer 2)      │
              │  Input: State [30D]      │
              │  Output: Action [0-4]    │
              └──────────┬───────────────┘
                         │
            ┌────────────┼────────────┐
            │            │            │
            ▼            ▼            ▼
      ┌─────────┐  ┌─────────┐  ┌─────────┐
      │ Enter   │  │ Enter   │  │  Wait   │
      │  Full   │  │  Half   │  │  (Skip) │
      └────┬────┘  └────┬────┘  └────┬────┘
           │            │             │
           └────────────┴─────────────┘
                        │
                        ▼
              ┌──────────────────────────┐
              │  EXECUTE TRADE ON MT5    │
              │  - Calculate position    │
              │  - Set SL/TP             │
              │  - Place order           │
              │  - Log to database       │
              └──────────┬───────────────┘
                         │
                         ▼
              ┌──────────────────────────┐
              │  MONITOR POSITION        │
              │  - Track P&L             │
              │  - Check RL for exits    │
              │  - Trail stops           │
              └──────────────────────────┘
```

---

## Rule Execution Engine (How It Works)

```
┌─────────────────────────────────────────────────────────────┐
│                    User's Strategy                          │
│  "Buy when RSI < 30 and price crosses above 50 EMA"         │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
         ┌────────────────────────────┐
         │      NLP PARSER            │
         │  Extract indicators + rules│
         └────────────┬───────────────┘
                      │
                      ▼
         ┌────────────────────────────┐
         │   Structured Rules JSON    │
         │  {                         │
         │    indicators: [           │
         │      {name:'RSI', period:14│
         │      {name:'EMA', period:50│
         │    ],                      │
         │    entry_conditions: [     │
         │      {type:'rsi_below',    │
         │       variables:{thresh:30}│
         │      {type:'cross_above',  │
         │       variables:{ind:'ema50│
         │    ]                       │
         │  }                         │
         └────────────┬───────────────┘
                      │
          ┌───────────┴───────────┐
          │                       │
          ▼                       ▼
┌──────────────────────┐  ┌──────────────────────┐
│ IndicatorCalculator  │  │   RuleEvaluator      │
│ ─────────────────── │  │ ───────────────────  │
│                      │  │                      │
│ For each indicator:  │  │ For each condition:  │
│                      │  │                      │
│ if name == 'RSI':    │  │ if type=='rsi_below':│
│   df['rsi14'] =      │  │   check if           │
│   calculate_rsi(     │  │   df[idx]['rsi14']   │
│     period=14)       │  │   < threshold        │
│                      │  │                      │
│ if name == 'EMA':    │  │ if type=='cross_above│
│   df['ema50'] =      │  │   check if crossed   │
│   df['close'].ewm(   │  │   above indicator    │
│     span=50).mean()  │  │                      │
│                      │  │                      │
│ ✅ GENERIC           │  │ ✅ DYNAMIC           │
│ Works for any        │  │ No code generation   │
│ indicator            │  │ Safe evaluation      │
└──────────┬───────────┘  └──────────┬───────────┘
           │                         │
           └──────────┬──────────────┘
                      │
                      ▼
         ┌────────────────────────────┐
         │  DataFrame with All Data   │
         │                            │
         │  timestamp | close | rsi14 │ ema50 │
         │  ──────────────────────────────────│
         │  10:00     | 1.10  | 28.5  │ 1.09  │
         │  11:00     | 1.11  | 32.1  │ 1.09  │
         │  12:00     | 1.12  | 45.2  │ 1.10  │
         │                            │
         └────────────┬───────────────┘
                      │
                      ▼
         ┌────────────────────────────┐
         │   Evaluate Each Candle     │
         │                            │
         │  for i in range(len(df)):  │
         │    is_valid, reason =      │
         │      evaluator.evaluate(   │
         │        df, i, conditions)  │
         │                            │
         │    if is_valid:            │
         │      ✅ SIGNAL FOUND!      │
         └────────────────────────────┘
```

---

## Feature Engineering (Automatic)

```
┌──────────────────────────────────────────────────────────────┐
│               User's Indicators                               │
│  [RSI14, EMA50, MACD, ATR14, BB20]                           │
└─────────────────────┬────────────────────────────────────────┘
                      │
                      ▼
         ┌────────────────────────────┐
         │  Feature Auto-Generator    │
         │                            │
         │  For each indicator:       │
         │    Generate features       │
         └────────────┬───────────────┘
                      │
      ┌───────────────┼───────────────────┬──────────────┐
      │               │                   │              │
      ▼               ▼                   ▼              ▼
┌──────────┐  ┌─────────────┐   ┌─────────────┐  ┌─────────────┐
│   RSI14  │  │    EMA50    │   │    MACD     │  │   ATR14     │
│ ──────── │  │ ───────────  │   │ ───────────  │  │ ──────────  │
│ rsi14    │  │ ema50       │   │ macd_line   │  │ atr14       │
│ rsi_slope│  │ ema50_slope │   │ macd_signal │  │ atr_ratio   │
│ rsi_ovrsl│  │ dist_to_ema │   │ macd_hist   │  │             │
│ rsi_ovrbt│  │ price>ema   │   │ macd_cross  │  │             │
└──────────┘  └─────────────┘   └─────────────┘  └─────────────┘
      │               │                   │              │
      └───────────────┴───────────────────┴──────────────┘
                      │
                      ▼
         ┌────────────────────────────┐
         │   Additional Context       │
         │ ─────────────────────────  │
         │ - Volatility (20 candles)  │
         │ - Trend strength           │
         │ - Candle body size         │
         │ - Candle wick ratio        │
         │ - Volume ratio             │
         └────────────┬───────────────┘
                      │
                      ▼
         ┌────────────────────────────┐
         │   Complete Feature Vector  │
         │                            │
         │  [rsi14, rsi_slope,        │
         │   ema50, dist_to_ema,      │
         │   macd_line, macd_hist,    │
         │   atr14, volatility,       │
         │   trend_strength, ...]     │
         │                            │
         │  Total: 25-45 features     │
         │  (depends on indicators)   │
         └────────────┬───────────────┘
                      │
                      ▼
         ┌────────────────────────────┐
         │    ML Model Training       │
         │  DNN learns from features  │
         └────────────────────────────┘
```

---

## Database Schema (User Isolation)

```
┌────────────────────────────────────────────────┐
│                User Table                      │
│  id | username | email                        │
├────────────────────────────────────────────────┤
│  1  | john     | john@example.com             │
│  2  | jane     | jane@example.com             │
└────────────────────────────────────────────────┘
          │                    │
          │                    └─────────────────┐
          │                                      │
          ▼                                      ▼
┌─────────────────────────┐          ┌─────────────────────────┐
│   UserStrategy          │          │   UserStrategy          │
│  (John's Strategies)    │          │  (Jane's Strategies)    │
├─────────────────────────┤          ├─────────────────────────┤
│ id: 1                   │          │ id: 3                   │
│ user_id: 1              │          │ user_id: 2              │
│ name: "RSI Strategy"    │          │ name: "MA Cross"        │
│ status: "live"          │          │ status: "training"      │
├─────────────────────────┤          ├─────────────────────────┤
│ id: 2                   │          │ id: 4                   │
│ user_id: 1              │          │ user_id: 2              │
│ name: "MACD Combo"      │          │ name: "BB Mean Rev"     │
│ status: "backtesting"   │          │ status: "ready"         │
└────────┬────────────────┘          └────────┬────────────────┘
         │                                    │
         ▼                                    ▼
┌─────────────────────────┐          ┌─────────────────────────┐
│  StrategyMLModel        │          │  StrategyMLModel        │
│  (John's Models)        │          │  (Jane's Models)        │
├─────────────────────────┤          ├─────────────────────────┤
│ strategy_id: 1          │          │ strategy_id: 3          │
│ model_path: "strat_1_ml"│          │ model_path: "strat_3_ml"│
│ accuracy: 0.68          │          │ accuracy: 0.72          │
├─────────────────────────┤          ├─────────────────────────┤
│ strategy_id: 2          │          │ strategy_id: 4          │
│ model_path: "strat_2_ml"│          │ model_path: "strat_4_ml"│
│ accuracy: 0.65          │          │ accuracy: 0.70          │
└─────────────────────────┘          └─────────────────────────┘

         ✅ ISOLATION:
         - John can't see Jane's strategies
         - Models trained independently
         - No data leakage
         - Performance tracked separately
```

---

## Cost & Infrastructure

```
┌──────────────────────────────────────────────────────────────┐
│                    TRAINING (Colab Pro)                       │
│  ────────────────────────────────────────────────────────── │
│  Weekly Sync:                                                │
│    1. Pull code from GitHub                                  │
│    2. Download user strategies from DB                       │
│    3. Fetch MT5 data                                         │
│    4. Train ML models (GPU)                                  │
│    5. Train RL agents (GPU)                                  │
│    6. Save models to Google Drive                            │
│    7. Sync back to VPS                                       │
│                                                              │
│  Cost: $10/month                                             │
│  Time: 1-2 hours per strategy                                │
└──────────────────────────────────────────────────────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │ Google Drive  │
                    │   Models      │
                    │ ───────────── │
                    │ strat_1_ml.pt │
                    │ strat_1_rl.zip│
                    │ strat_2_ml.pt │
                    │ strat_2_rl.zip│
                    └───────┬───────┘
                            │
                            ▼ (sync)
┌──────────────────────────────────────────────────────────────┐
│                    PRODUCTION (VPS - CPU)                     │
│  ────────────────────────────────────────────────────────── │
│  Real-Time:                                                  │
│    1. Monitor markets for all users                          │
│    2. Generate signals (rule engine)                         │
│    3. Load ML model for inference (CPU - fast enough!)       │
│    4. Load RL agent for decisions                            │
│    5. Execute trades on MT5                                  │
│    6. Track performance                                      │
│                                                              │
│  Cost: $40/month (8GB RAM, 4 cores)                          │
│  Inference: < 50ms per decision                              │
│  Scalable: 50-100 users per VPS                              │
└──────────────────────────────────────────────────────────────┘

         Total Cost: $50/month (training + production)
         Scales to: 100+ users before needing 2nd VPS
```

---

## Workflow Comparison: TCE vs User Strategy

```
┌──────────────────────────────────────────────────────────────┐
│                    TCE STRATEGY (Fixed)                       │
└──────────────────────────────────────────────────────────────┘

validation.py (hardcoded rules)
    ↓
calculate_indicators(ma6, ma18, ma50, ma200)
    ↓
validate_tce(candle, indicators, ...)
    ↓
extract_features(20 hardcoded features)
    ↓
train_ml_model(TCE_dataset)
    ↓
train_rl_agent(TCE_env)
    ↓
deploy_live(one strategy)


┌──────────────────────────────────────────────────────────────┐
│               USER STRATEGY (Dynamic)                         │
└──────────────────────────────────────────────────────────────┘

user_description (natural language)
    ↓
parse_with_llm(description) → rules
    ↓
calculate_indicators(user's indicators) [GENERIC]
    ↓
evaluate_conditions(user's rules) [DYNAMIC]
    ↓
extract_features(auto-generated from user indicators)
    ↓
train_ml_model(user_dataset)
    ↓
train_rl_agent(user_env)
    ↓
deploy_live(N strategies, isolated)


        ✅ SAME PIPELINE, DIFFERENT DATA!
```

This is why your no-code builder can reuse so much of TCE's infrastructure!

---

**These diagrams are now part of your documentation!** 📚
